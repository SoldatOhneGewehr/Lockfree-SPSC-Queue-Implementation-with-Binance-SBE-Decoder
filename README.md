# Lockfree-SPSC-Queue-Implementation-with-Binance-SBE-Decoder
A lockfree SPSC Queue implementation using Binance SBE data. The code takes binary inputs, decodes them according to schema offered by Binance and simply encodes and prints the stream.  
